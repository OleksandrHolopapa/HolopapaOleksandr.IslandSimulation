package org.javarush.Animals.Predators;

public class Boa extends Predator {
    public Boa(double maxWeight, int maxPopulation, int maxSpeedOfMovement, double maxCanEat) {
        super(maxWeight, maxPopulation, maxSpeedOfMovement, maxCanEat);
    }
}