package org.javarush.Animals.Predators;

import org.javarush.Animals.Animal;

public class Predator extends Animal {
    public Predator(double maxWeight, int maxPopulation, int maxSpeedOfMovement, double maxCanEat) {
        super(maxWeight, maxPopulation, maxSpeedOfMovement, maxCanEat);
    }
}